program Test9;

struct Vec2 {
    let x: float;
    let y: float;
}

function magnitude(v: Vec2): float {
    return (v.x * v.x + v.y * v.y);
}

main {
    let a: Vec2;
    a.x = 3.0;
    a.y = 4.0;
    print(magnitude(a));
}
